# Escreva um programa que leia números até o usuário digitar -1

def number_test(number):
     while number != -1:
          number = float(input("Informe um número:"))

input0 = float(input("Informe um número:"))

number_test(input0)
